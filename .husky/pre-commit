if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks not found on PATH — install it before committing (see README secrets section)." >&2
  exit 1
fi

gitleaks protect --staged --redact --config .gitleaks.toml -v
